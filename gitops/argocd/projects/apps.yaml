apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: apps
  namespace: argocd
spec:
  description: Application workloads (namespaced resources only; per-app prod namespaces allowed)
  sourceRepos:
    - https://github.com/humble92/argo-examples.git
    - https://github.com/devopsjourney1/argo-examples.git
  destinations:
    - namespace: prod
      server: https://kubernetes.default.svc
    - namespace: dev
      server: https://kubernetes.default.svc
    - namespace: staging
      server: https://kubernetes.default.svc
    - namespace: n8n-prod
      server: https://kubernetes.default.svc
  clusterResourceWhitelist: []
  namespaceResourceWhitelist:
    - group: "*"
      kind: "*"
  # Bootstrap Secret n8n-prod-secrets is created out-of-band (see README); not in Git.
  orphanedResources:
    warn: true
    ignore:
      - group: ""
        kind: Secret
        name: n8n-prod-secrets
