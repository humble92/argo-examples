apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Projects (must exist before Applications)
  - projects/infrastructure.yaml
  - projects/apps.yaml
  # Infrastructure Applications (sync-wave order: -50 .. -20; ClusterIssuers ride cert-manager multi-source)
  - infrastructure/envoy-gateway-crds.yaml
  - infrastructure/cert-manager.yaml
  - infrastructure/envoy-gateway.yaml
  - infrastructure/envoy-edge.yaml
